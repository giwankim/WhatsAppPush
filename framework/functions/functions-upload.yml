GetSignedUrl:
  handler: src/upload/handlers/getSignedUrl.handler
  description: Function to get s3 bucket predsigned url to upload recipient file
  events:
    - http:
        path: /upload-url/{userId}
        method: POST
        cors: true
